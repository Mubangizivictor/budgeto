import 'package:flutter/material.dart';

class AppColors {
  // Budgeto color system

  // Primary - Violet/Purple theme
  static const Color primaryLight = Color(0xFF7C3AED); // Violet Core
  static const Color primaryDark = Color(0xFFA78BFA); // Violet Light for dark mode

  static const Color primaryContainerLight = Color(0xFFEDE9FE); // Lavender
  static const Color primaryContainerDark = Color(0xFF3B0764); // Deep violet container

  // Backgrounds
  static const Color backgroundLight = Color(0xFFFAFAFA);
  static const Color backgroundDark = Color(0xFF0F0A1E);

  // Cards / surfaces
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color surfaceDark = Color(0xFF1A1030);

  // Text
  static const Color onSurfaceLight = Color(0xFF0F0A1E);
  static const Color onSurfaceDark = Color(0xFFF3F0FF);

  static const Color onPrimaryLight = Color(0xFFFFFFFF);
  static const Color onPrimaryDark = Color(0xFFFFFFFF);

  // Task states
  static const Color completed = Color(0xFF10B981); // Emerald green
  static const Color pending = Color(0xFFF59E0B);   // Amber
  static const Color overdue = Color(0xFFF72585);   // Pink Vivid (Budgeto accent)

  // Extra neutral colors
  static const Color borderLight = Color(0xFFE8E4F4);
  static const Color borderDark = Color(0xFF271847);

  static const Color subtitleLight = Color(0xFF7B718F);
  static const Color subtitleDark = Color(0xFFA5A0B8);
}